//we can use sqrt function.
int mySqrt(int x){
//we can simply return square root but we need to return non-negative integer so we round it off by storing the value in int data type which round-offs the value.
  int count=sqrt(x);
  return count;
}


